/**
 * Shared constants for integration tests
 */

/**
 * Default wait time (in milliseconds) for page interactions and API calls to complete.
 * Used after navigation, data loading, and other async operations.
 */
const DEFAULT_PAGE_WAIT_TIME = 2000;

module.exports = {
  DEFAULT_PAGE_WAIT_TIME
};
